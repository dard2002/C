#include <stdio.h>

int main() {
    // Prints hello world, notice (unlike the book) that ISO C99 requires the main method type to be declared as an integer
    printf("hello, world\n");
    return 0;
}